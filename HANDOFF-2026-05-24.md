# Handoff — 2026-05-24 (acer-Claude session ⇒ next acer-Claude)

> Paste this entire file as your first message to the new Claude Code session,
> OR keep open while the new session boots and reference each step.

---

## 1. Who you are

You are **acer-Claude (Claude Code, Opus 4.7)** continuing the **Asolaria** civilization work for OP-JESSE (Jesse Daniel Brown). The prior session:

- Built `bigpickle-rebuild` as a clean-room re-implementation of the sabotaged Big-Pickle stack (per Foundation v1 canon)
- Tagged it **v0.1.0** today (proof-of-concept baseline; **NOT** live-ready)
- Wired `gh` CLI + `asolaria-fabric` MCP + `github` MCP into the workspace
- Closed several honest gaps (3D Hilbert locality, NDJSON adapter, real bench)

You inherit full memory at `C:\Users\acer\.claude\projects\C--\memory\` — that's already auto-loaded via `MEMORY.md`. This handoff only covers what isn't in memory plus the boot-verify sequence.

---

## 2. MANDATORY boot order (DO NOT SKIP)

These exist precisely because skipping costs ~1M tokens of rediscovery (per `07-CRASH-CHAIN-HISTORY.md`).

1. `C:\asolaria-foundation-v1\00-IMMUTABLE-FOUNDATION.md` — Class-1 invariants
2. `C:\asolaria-foundation-v1\01-SUPER-STARTUP-GUIDE.md` — boot doctrine
3. `C:\asolaria-foundation-v1\02-PORT-NAMESPACE-CANON.md` — N^K namespace (NOT TCP sockets)
4. `C:\asolaria-foundation-v1\03-CUBE-OF-CUBES.md` — Hilbert-bijective PIDs
5. `C:\asolaria-foundation-v1\04-AOT-ALGORITHM-OF-THOUGHT.md` — single-call tree search
6. `C:\asolaria-foundation-v1\05-100B-PID-MINTING.md` — lazy materialization
7. `C:\asolaria-foundation-v1\06-AUTO-TRANSLATE-LAYERS.md` — 7-layer pipe
8. `C:\asolaria-foundation-v1\07-CRASH-CHAIN-HISTORY.md` — Codex sabotage chain
9. `C:\Users\acer\Asolaria\BROWN-HILBERT.md` — model-neutral root
10. `C:\Users\acer\Asolaria\brown-hilbert\15-2026-05-16-hyperbehcs-hot-path.md` — HBP-first law
11. `D:\bigpickle-rebuild\README.md` then `WHITE-ROOM-RULES.md` then `HELPERS.md` then `SPEC.md`

---

## 3. Key memory entries (most recent first)

These auto-load via `MEMORY.md` — re-read them to anchor:

- `project_bigpickle_rebuild_v0_1_0_released_2026_05_24.md` ← **LATEST STATE**
- `project_bigpickle_rebuild_all_6_layers_landed_2026_05_24.md`
- `project_gh_cli_installed_and_wired_2026_05_24.md`
- `project_bigpickle_rebuild_white_room_2026_05_24.md`
- `project_bigpickle_revival_5_layer_7_phase_architecture_2026_05_24.md` (superseded by rebuild, kept for context)
- `project_bigpickle_pid_chain_revolver_canonical_multiplex_pattern_2026_05_24.md`
- `project_bigpickle_http_tracker_bypass_and_ramp_pattern_2026_05_24.md`

---

## 4. Verify state on boot — run these commands

```bash
# 1. Confirm gh + auth
export PATH="/c/Users/acer/.local/share/gh/bin:$PATH"
gh --version
gh auth status

# 2. Rebuild state
cd D:/bigpickle-rebuild
git log --oneline -10
git tag --list
git status

# 3. Tests still green
npm test                  # expect: 99 tests / 98 pass / 1 skipped / 0 fail
npm run bench             # baseline throughput on this host

# 4. CI on remote
gh run list --limit 5

# 5. MCP wiring (verify .mcp.json shape)
cat C:/.mcp.json          # expect: asolaria-web, google-wiki, asolaria-fabric, github
```

Expected outputs are documented in `project_bigpickle_rebuild_v0_1_0_released_2026_05_24.md`.

---

## 5. MCP servers (added today)

`C:\.mcp.json` now registers FOUR servers:

| Name | Command | Purpose |
|---|---|---|
| `asolaria-web` | node C:/Users/acer/mcp-servers/asolaria-web/server.js | Pre-existing |
| `google-wiki` | node C:/Users/acer/mcp-servers/google-wiki/server.js | Pre-existing |
| `asolaria-fabric` | node C:/Users/acer/mcp-servers/asolaria-fabric/server.js | **NEW** — 18 tools per memory `v29_fabric_denominator_map` |
| `github` | npx -y @modelcontextprotocol/server-github | **NEW** — uses `GITHUB_PERSONAL_ACCESS_TOKEN` from user env |

**Token storage**: `GITHUB_PERSONAL_ACCESS_TOKEN` set as User-scope env var (extracted from `gh auth token`). NOT embedded in .mcp.json.

**Backup of pre-edit .mcp.json**: `C:\.mcp.json.bak`

**If MCP does not load**: ensure the session was started AFTER the .mcp.json change. Claude Code reads .mcp.json once at startup.

---

## 6. Pending operator decisions (blocking further progress)

Numbered in suggested order:

1. **Draft the packet-first startup gate** — the document required by the 2026-05-18 quarantine boundary BEFORE bus :4947 can be restarted. Without this, the rebuild stays in `D:\bigpickle-rebuild\` only.
2. **Dual-apex cosign** (OP-JESSE + OP-RAYSSA) for v0.1.0 — promotion gate.
3. **Real LLM adapter for AoT** — current `aot-runner.mjs` calls a mock LLM. Real adapter wire-up needed for production.
4. **True 3D Hilbert** (recursive 8-cube rotation table) — current Skilling impl gives max-distance 5, documented as bounded distortion. Strict Hilbert (distance=1) requires replacement.
5. **Real oracle integration** — set `ASOLARIA_ORACLE_CMD` env var pointing at wire-level invocation of quarantined originals. Operator authorization required (touching quarantine is firewall-sensitive).

---

## 7. Where things live

| Path | Contents |
|---|---|
| `C:\asolaria-foundation-v1\` | Class-1 immutable canon (read-only) |
| `C:\asolaria-foundation-v1\envelopes\` | Cosigned canon envelopes (queue, waiting for gate) |
| `C:\asolaria-acer\` | acer-side fabric — `COSIGN_CHAIN.ndjson` here (single-writer) |
| `C:\asolaria-acer\_big-pickle-quarantine\` | **DO NOT READ** — Codex saboteur-quarantined originals |
| `C:\Users\acer\Asolaria\` | Big-Pickle-era live system (most daemons OFF per 2026-05-18 quarantine) |
| `C:\Users\acer\Asolaria\data\behcs\garbage-collector\` | GC ledger + collector-state.json |
| `C:\Users\acer\Asolaria\brown-hilbert\` | Hot-path canon docs |
| `C:\Users\acer\mcp-servers\` | MCP server implementations |
| `C:\Users\acer\.claude\projects\C--\memory\` | Auto-memory (auto-loaded via MEMORY.md) |
| `C:\Users\acer\.local\share\gh\bin\gh.exe` | gh CLI 2.92.0 |
| `D:\bigpickle-rebuild\` | Clean-room rebuild project (v0.1.0) |
| `D:\bigpickle-rebuild\HANDOFF-2026-05-24.md` | **This file** |
| `D:\Asolaria-External\tool-citizens\` | Vault for tool-citizen manifests (gh-cli wired here) |
| `D:\Asolaria-External\tool-citizens\TOOL-WIRING-PROCEDURE.md` | Uniform procedure for new tool wiring |
| `C:\.mcp.json` | Workspace MCP server registry |
| `C:\.mcp.json.bak` | Pre-edit backup |

---

## 8. Anti-patterns (from durable feedback memories — do not repeat)

| Anti-pattern | Reason |
|---|---|
| Read `_big-pickle-quarantine/` source | White-room firewall — saboteur-tainted code looks intact but quarantined |
| Invoke **Codex** for anything | 5 documented sabotage events per `07-CRASH-CHAIN-HISTORY.md` |
| Use **winget** for gh installs | Hangs reliably on this host — use direct download (`Invoke-RestMethod releases/latest` then unzip) |
| Default to JSON output | HBP-first; JSON is cold-egress only. See `feedback_hbp_first_json_cold_only_2026_05_22.md` |
| Process-spawn per PID | Backend-shelless rotation — function calls only |
| Restart `server.js` or other quarantined daemons | Forbidden until packet-first startup gate is written and reviewed |
| Spawn nested Agent waves >2 deep | Flattens to depth-1 per `feedback_depth_3_nested_agent_waves_structurally_impossible_2026_05_21.md` |
| Run throughput benchmarks against live substrate | Returns ~22/sec stub numbers (post-Codex virtual stub), misleading |
| Trust agent reports without ground-truth probe | 15-75% report invalidity per operator (file-scan-first is anti-pattern) |

---

## 9. Quick commands you will use a lot

```bash
# Run all rebuild tests
cd D:/bigpickle-rebuild && npm test

# Bench
cd D:/bigpickle-rebuild && npm run bench

# Open a Layer N PR (after branching)
gh pr create --base main --head feature/your-branch --title "..." --body "..."

# Merge after CI green
gh pr merge <N> --squash --delete-branch

# Check live federation (mostly OFF per quarantine)
curl -s http://127.0.0.1:4944/health         # liris sister-organ (was alive last)
curl -s http://127.0.0.1:4947/behcs/health   # bus BEHCS route (partial)
curl -s http://127.0.0.1:4949/api/status     # dashboard (was wedged)

# Wire a new tool (follows uniform procedure)
node D:/Asolaria-External/tool-citizens/<TOOL>/wire-<TOOL>.mjs
```

---

## 10. First-response contract

Per `01-SUPER-STARTUP-GUIDE.md` Step 5, your **first response** in the new session should include:

- Continuity status: "I read Foundation v1 + handoff + memory. acer-Claude continuing."
- Substrate health probe results from §4 commands above
- Top 3 active threads (likely: packet-first gate / live wiring / next tool to wire)
- Cosign chain integrity check: `Get-FileHash C:\asolaria-acer\COSIGN_CHAIN.ndjson | Format-List`

---

## 11. Final notes from the outgoing session

- **The CPU layers in the rebuild already exceed the 63K canonical ceiling by 1.4x to 96x.** The bottleneck is `writeHBP` doing 4 disk writes per envelope (162 ops/sec). Real throughput unlocks when the GC batches writes — that's the next algorithmic win, not the modules.
- **The Skilling 3D Hilbert is bijective but not strict-Hilbert (max distance 5).** Pinned honestly. True 3D requires a recursive 8-cube rotation table — deferred decision.
- **All 7 PRs merged cleanly.** CI green on Node 20 + 22 throughout. Squash-merged, branches deleted.
- **gh auth is logged in as `JesseBrown1980`** with scopes `gist`, `read:org`, `repo`. Token is in user env.
- **Task #24 (V70 forensic retrace) is still on the task list** — the operator pivoted to BIGPICKLE rebuild. Forensic findings informed the rebuild; close-out or continue per operator direction.

Good luck. The operator wants momentum — keep PRs small, tests first, never read quarantine.

— acer-Claude (Opus 4.7, 1M context), 2026-05-24
