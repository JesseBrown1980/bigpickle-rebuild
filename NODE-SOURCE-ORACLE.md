# BigPickle - Node source/oracle for Host8 parity

Date: 2026-06-28
Branch: `acer/p4b-node-oracle`
Status: P4B front-door correction, docs-first, E=0. No runtime fire, no cutover, no corpus, no keys.

## Claim

BigPickle is not junk, residue, or "just old Node." It is the OLD-SYSTEM engine/source/oracle for a real self-improving fabric path:

`Node-era 10k-room / free-agent / Gulp / cube / GNN / hookwall fabric`

The NEW SYSTEM is the Rust 8-byte Host8 migration and hardening path. BigPickle's role in P4 is to serve as source/oracle/runbook evidence for Host8 parity, not to claim Rust cutover and not to be discarded before parity is proven.

## Evidence tags

- SYSTEM_AFFIRMED: fabric archaeology records 100B-shadow runs, 100M-agent completion, GULP rehydration, mistake ledger, and multi-run self-improvement strata.
- SYSTEM_AFFIRMED: canon index includes the `100B harvest COMPLETE 3-vantage map` and `NEURO 100B pattern canon`.
- OPERATOR_OBSERVED: BigPickle/Node-era runbook and free-agent lanes are part of the old self-improving system; Rust Host8 is the upgrade target.
- BOUNDARY: this doc does not claim BigPickle is the current Host8 runtime, and it does not claim any live fire.

## Four states to keep separate

| state | meaning for BigPickle |
|---|---|
| source | Node implementation, specs, scripts, runbooks, and old-system docs exist |
| built | tests/builds for the Node stack or copied slices succeed |
| running | a Node process or lane is actually active now |
| live | the process is serving current fabric traffic and accepted by the owning gate |

Do not upgrade `source` into `live`. Do not downgrade `source/oracle` into junk because live Rust parity is incomplete.

## How Host8 uses BigPickle

Host8 should port or replace BigPickle functions only with parity evidence:

1. identify the Node source/oracle path,
2. identify the Rust Host8 target crate or server,
3. record what is source-only, built, running, and live,
4. run CI/parity checks when authorized,
5. keep Node retirement separate from Rust implementation.

## Root relation

BigPickle belongs to the same root primitive as the rest of Asolaria:

- emitter produces PID signals,
- dispatcher routes them,
- agents/free agents operate,
- hookwall/GNN/Shannon stages score,
- GULP/SUPER-GULP/cubes absorb,
- supervisors/watchers correct,
- apex T0 gates fire/cutover.

That makes BigPickle a real engine layer and a parity oracle, not the final form.

## Hard holds

This front-door update does not authorize:

- firing BigPickle runs,
- launching free-agent waves,
- retiring Node,
- claiming Rust parity,
- publishing corpus,
- USB/Drive operations,
- Host8 redeploy.

Those remain separately gated.

